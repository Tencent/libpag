/**
 * Marker stores comments and other metadata and mark important times in a composition or layer.
 */
export class PAGMarker {
  startTime: number = 0;
  duration: number = 0;
  comment: string = "";
}